tsc -v
node -v
.js เป็นแบบ Dynamic type
let str = 'hello'
console.log('str',typeof str)
str = 10 
console.log('str',typeof str)
str = true
console.log('str',typeof str)

ใช้คำสั่ง "run" node app.js เพื่อเช็ค type of

.ts เป็บแบบ Static type
ใช้ "Run" appts.ts ไม่ได้ต้อง ต้อง compiler ก่อน 
การ compiler ใช้ tsc appts.ts
ใช้คำสั่ง "run" node appts.ts ได้แล้ว
